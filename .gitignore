sessions/
__pycache__/
*.pyc
*.egg-info/
dist/
build/
.venv/
vendor/pyLMUSharedMemory/.git
.claude/*
# ...but track shared project skills (settings.local.json etc. stay ignored)
!.claude/skills/
=*
.worktrees/
.superpowers/
.pytest-tmp*/
tmp/

# Update-repo tooling: private signing keys and local repo state. The keys are
# secret (BACK THEM UP offline); the repository/ holds large target archives
# and TUF metadata published to the host instead of git. The public trust
# anchor packaging/trusted/root.json IS committed.
packaging/keys/
packaging/repository/
packaging/bundle/
packaging/.tufup-repo-config
